<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.msa4lms.domain.lecture.mapper.ProfessorLectureMapper">

    <sql id="lectureSelectFields">
        lectures.id,
        courses.id AS course_id,
        courses.code AS course_code,
        courses.name AS course_name,
        courses.credits,
        courses.target_grade AS target_grade,
        departments.name AS department_name,
        users.name AS professor_name,
        lectures.classroom,
        COALESCE(sched.schedule_str, '') AS schedule,
        lectures.capacity,
        COALESCE(enrl.current_enrollment, 0) AS current_enrollment,
        semesters.academic_year,
        semesters.term,
        lectures.midterm_ratio,
        lectures.final_ratio,
        lectures.assignment_ratio,
        lectures.attendance_ratio,
        lectures.syllabus
    </sql>

    <sql id="lectureJoins">
        JOIN courses ON lectures.course_id = courses.id
        JOIN departments ON courses.department_id = departments.id
        JOIN professors ON lectures.professor_id = professors.id
        JOIN users ON professors.user_id = users.id
        JOIN semesters ON lectures.semester_id = semesters.id
        LEFT JOIN (
            SELECT lecture_id, GROUP_CONCAT(
                CONCAT(
                    CASE day_of_week
                        WHEN 'MON' THEN '월요일' WHEN 'TUE' THEN '화요일' WHEN 'WED' THEN '수요일'
                        WHEN 'THU' THEN '목요일' WHEN 'FRI' THEN '금요일' ELSE day_of_week
                    END,
                    ' ',
                    LPAD(start_period + 8, 2, '0'), ':00 ~ ',
                    LPAD(end_period + 8, 2, '0'), ':50'
                ) SEPARATOR ', '
            ) AS schedule_str
            FROM lecture_schedules
            GROUP BY lecture_id
        ) sched ON lectures.id = sched.lecture_id
        LEFT JOIN (
            SELECT lecture_id, COUNT(*) AS current_enrollment
            FROM enrollments
            WHERE status = 'ACTIVE'
            GROUP BY lecture_id
        ) enrl ON lectures.id = enrl.lecture_id
    </sql>

    <select id="findDepartmentIdByProfessorId" resultType="java.lang.Long">
        SELECT department_id FROM professors WHERE user_id = #{professorId}
    </select>

    <insert id="insertCourse" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO courses (code, name, credits, department_id, target_grade, completion_type)
        VALUES (#{code}, #{name}, #{credits}, #{departmentId}, #{targetGrade}, #{completionType})
    </insert>

    <insert id="insertLecture" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO lectures (
            semester_id, course_id, professor_id, section_no, capacity, classroom, status,
            midterm_ratio, final_ratio, assignment_ratio, attendance_ratio, syllabus
        ) VALUES (
            #{semesterId}, #{courseId}, (SELECT id FROM professors WHERE user_id = #{professorId}), #{sectionNo}, #{capacity}, #{classroom}, 'OPEN',
            #{midtermRatio}, #{finalRatio}, #{assignmentRatio}, #{attendanceRatio}, #{syllabus}
        )
    </insert>

    <insert id="insertLectureSchedule">
        INSERT INTO lecture_schedules (lecture_id, day_of_week, start_period, end_period)
        VALUES (#{lectureId}, #{schedule.dayOfWeek}, #{schedule.startPeriod}, #{schedule.endPeriod})
    </insert>

    <select id="findLecturesByProfessor" resultType="com.msa4lms.domain.lecture.responses.LectureRes">
        SELECT <include refid="lectureSelectFields" />
        FROM lectures
        <include refid="lectureJoins" />
        WHERE lectures.professor_id = (SELECT id FROM professors WHERE user_id = #{professorId})
        ORDER BY semesters.academic_year DESC, semesters.term DESC, courses.name ASC
    </select>

    <select id="findLecturesByProfessorId" resultType="com.msa4lms.domain.lecture.responses.LectureRes">
        SELECT <include refid="lectureSelectFields" />
        FROM lectures
        <include refid="lectureJoins" />
        WHERE lectures.professor_id = (SELECT id FROM professors WHERE user_id = #{professorId})
        <if test="year != null">
            AND semesters.academic_year = #{year}
        </if>
        <if test="semester != null">
            AND semesters.term = IF(#{semester} = 1, 'FIRST', 'SECOND')
        </if>
        ORDER BY semesters.academic_year DESC, semesters.term DESC, courses.name ASC
    </select>

    <select id="findPastLecturesByProfessorId" resultType="com.msa4lms.domain.lecture.responses.LectureRes">
        SELECT <include refid="lectureSelectFields" />
        FROM lectures
        <include refid="lectureJoins" />
        WHERE lectures.professor_id = (SELECT id FROM professors WHERE user_id = #{professorId})
        ORDER BY semesters.academic_year DESC, semesters.term DESC, courses.name ASC
    </select>

    <select id="findAvailableCoursesForProfessor" resultType="com.msa4lms.domain.lecture.responses.CourseRes">
        SELECT c.id, c.code, c.name, c.credits, c.target_grade AS targetGrade, c.completion_type AS completionType
        FROM courses c
        JOIN professors p ON c.department_id = p.department_id
        WHERE p.user_id = #{professorId}
        ORDER BY c.target_grade ASC, c.name ASC
    </select>

</mapper>
