<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.msa4lms.domain.professor.mapper.ProfessorMapper">
    <select id="findProfileByUserId"
            resultType="com.msa4lms.domain.professor.responses.ProfessorProfileRes">
        SELECT
            u.name AS name,
            u.status AS status,
            d.name AS departmentName,
            u.login_id AS professorNo,
            u.email AS email,
            p.hire_year AS hireYear
        FROM professors p
            JOIN users u ON p.user_id = u.id
            JOIN departments d ON p.department_id = d.id
        WHERE p.user_id = #{userId}
    </select>
</mapper>