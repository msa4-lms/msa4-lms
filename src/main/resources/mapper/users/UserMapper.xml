<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.msa4lms.domain.user.mapper.UserMapper">
    <resultMap id="UserResultMap" type="com.msa4lms.domain.user.entities.User">
        <id column="id" property="id"/>
        <result column="user_no" property="userNo"/>
        <result column="name" property="name"/>
        <result column="email" property="email"/>
        <result column="password" property="password"/>
        <result column="role" property="role"/>
        <result column="department_id" property="departmentId"/>
        <result column="refresh_token" property="refreshToken"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted_at" property="deletedAt"/>
    </resultMap>


    <update
            id="updatePassword"
        >
        UPDATE users
        SET password = #{password},
            refresh_token = NULL,
            updated_at = NOW()
        WHERE id = #{id}
            AND deleted_at IS NULL
    </update>

    <select id="findByLoginId" resultMap="UserResultMap">
        SELECT
            *
        FROM users
        WHERE login_id = #{loginId}
          AND deleted_at IS NULL
    </select>

    <select id="findByPk" resultMap="UserResultMap">
        SELECT
            *
        FROM users
        WHERE
            id = #{id}
          AND deleted_at IS NULL
    </select>
</mapper>
